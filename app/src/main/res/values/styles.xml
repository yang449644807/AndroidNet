<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Base application theme. -->
    <style name="BaseAppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/btn_text_color</item>
    </style>

    <style name="DefaultTitleBarStyle">
        <item name="left_text">右边</item>
        <item name="left_text_visible">gone</item>
        <item name="left_img_src">@mipmap/ic_back</item>
        <item name="left_img_visible">visible</item>
        <item name="title_text">我是标题</item>
        <item name="right_text">右边</item>
        <item name="right_text_visible">visible</item>
        <item name="right_img_visible">gone</item>
    </style>
</resources>
